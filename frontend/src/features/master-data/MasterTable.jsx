export default function MasterTable({ title, columns, rows, onCreate, onEdit, onToggleActive }) {
  return <section className="master-table panel"><div className="table-heading"><div><p className="eyebrow">Master data</p><h2>{title}</h2></div><button className="primary-button" onClick={onCreate}>Tambah {title}</button></div><div className="table-wrap"><table><thead><tr>{columns.map((column) => <th key={column.key}>{column.label}</th>)}<th>Status</th><th>Aksi</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}>{columns.map((column) => <td key={column.key}>{column.render ? column.render(row) : row[column.key]}</td>)}<td><span className={`status-badge ${row.isActive ? 'active' : 'inactive'}`}>{row.isActive ? 'Aktif' : 'Nonaktif'}</span></td><td><div className="action-group"><button className="secondary-button" onClick={() => onEdit(row)}>Edit</button><button className="text-button" onClick={() => onToggleActive(row)}>{row.isActive ? 'Nonaktifkan' : 'Aktifkan'}</button></div></td></tr>)}{rows.length === 0 && <tr><td className="empty-cell" colSpan={columns.length + 2}>Belum ada data. Tambahkan {title.toLowerCase()} pertama.</td></tr>}</tbody></table></div></section>
}
